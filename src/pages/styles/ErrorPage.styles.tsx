import { styled } from "@mui/material/styles";
import { Paper } from "@mui/material";

export const Card = styled(Paper)`
  padding: 20px;
  p {
    color: red;
    font-size: 20px;
    font-weight: 500;
  }
`;
